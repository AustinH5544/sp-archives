# SP-ARCHIVES — architecture decisions and roadmap

Running record of what was decided and why. Updated 2026-09-12.

## Hosting

Cloudflare **Workers Static Assets**, not Pages.

Pages is not deprecated and has no announced end-of-life. Cloudflare's docs
say "Start new projects with Workers" — a steer, not a forced migration. We
moved because Workers has the broader feature set, not because Pages is
going away. `sp-archives.pages.dev` still exists as a fallback.

`wrangler.jsonc` deliberately has **no `main` key**. With no Worker script,
asset requests are served directly and are free/unbilled. Adding a script
(for a contact-form API, or to proxy R2) turns every matching request into a
billed Worker invocation. Weigh that before adding one.

Live on `sp-archives.com` and `www.sp-archives.com` as custom domains, so
the Worker is the origin and Cloudflare issues the certificates itself.

The account is **Skyelar's** (`68fd0549e41d1746b1ddb0ed5ce0f4b6`), not
Austin's. Austin currently has Super Administrator + Billing, which is more
than needed — should be reduced to Administrator. Only Skyelar can do that.

## CI/CD

GitHub connected via **Workers Builds** (dashboard-only; there is no API for
the connection). Production branch `master`, build `npm run build`, deploy
`npx wrangler deploy`.

Non-production branch builds are enabled. These run `npx wrangler versions
upload`, **not** `wrangler deploy` — so pushing a feature branch uploads a
version but never promotes it to production. Only `master` goes live.

The Worker name in the dashboard must match `name` in `wrangler.jsonc` or
the build fails. Both are `sp-archives`.

## Caching (`public/_headers`)

Cloudflare's default for every static asset is
`Cache-Control: public, max-age=0, must-revalidate`. Correct for HTML, but
it meant a returning visitor re-validated all 36 plates of a gallery on
every load — 36 round trips each returning 304 and zero bytes.

- `/_next/static/*` → one year, `immutable`. Next content-hashes these, so a
  changed file is a changed URL. Also covers the self-hosted fonts.
- `/work/*` → one day, **not** `immutable`. The photos are `01.jpg`,
  `cover.jpg` — no content hash. `immutable` would mean a replaced frame
  never reaches a returning visitor. Rename the file if a swap must be seen
  immediately.
- `workers.dev` preview URLs → `X-Robots-Tag: noindex`.

`.gitattributes` pins this file to LF so the Cloudflare parser sees clean
lines regardless of platform.

Note: `_headers` does **not** apply to responses generated by Worker code,
and does not apply to a different hostname. If photos move to an R2 custom
domain, the `/work/*` rule becomes dead and must be deleted.

## Image quality — SHIPPED 2026-09-12

Storage location was never the quality lever. The ceiling was
`images.unoptimized: true`, which shipped one file to every device: no
srcset, no format negotiation. That is now replaced.

**Masters.** 163 frames from three shoots, 4000-6240px, sRGB embedded on
every one, 4:4:4 chroma, 1.86 GB. Skyelar hit the export brief exactly.
About 20 frames are under 4000px (square crops) and cannot go full-bleed
on a large display.

**Storage.** R2 bucket `sp-archives-photos` (WNAM), served through the
custom domain `img.sp-archives.com`. 1.86 GB of the 10 GB free tier, zero
egress cost. Nothing in git.

**Delivery.** Cloudflare Images transformations, `format=auto`, via a
Next custom loader at `src/lib/image-loader.ts`.

### Four decisions that are load-bearing

**Content-addressed keys** — `<slug>/<NN>-<hash8>.jpg`. Browsers cache the
*transformed* URL and that URL embeds the source. A stable key like
`01.jpg` would strand a replaced photo behind its old copy permanently.

**Fixed width ladder**, never viewport-derived. Cloudflare bills per
*unique* transformation; widths computed from the real viewport would mint
one per visitor. 163 x 8 widths stays well inside the free 5,000/month.

**quality=85 is a floor, not a tuning knob.** Skyelar's edits carry film
grain. Compared at 1:1, q95 and q85 are near identical; q78 visibly
smooths skin and flattens fabric; q65 is plastic. Going below 85 deletes
something he put there on purpose. This is the opposite of the usual
"q75 is fine" advice and it is specific to his style.

**AVIF stops at 1200px.** Cloudflare serves WebP above that and ignores
`format=avif` even when forced. So the small end of the ladder gets AVIF's
compression win and the large end does not. Not a bug, do not chase it.

### Zone configuration

Transformations are enabled on `sp-archives.com` with Sources set to
**"This zone only"**. `img.sp-archives.com` qualifies as `*.sp-archives.com`.
Do not switch to "Any origin" — that lets third parties transform their
images on this zone's quota.

Because of that setting the loader must pass foreign origins through
untransformed. picsum placeholders and localhost are both bypassed; without
that, every placeholder would 404 the moment the loader went live.

### Privacy note

`src/lib/photos.json` carries only key/width/height. The uploader's full
manifest also records original filenames, which contain client names
(`Rielee-01.jpg`). That file is imported into the client bundle, so those
are stripped deliberately. Do not add them back.

### Re-running

`node scripts/upload-photos.mjs <manifest.json>` uploads and writes a
manifest; `--dry` previews, `--limit N` caps. Source paths MUST stay
double-quoted in the command it builds: several files are named like
`MaterneB&W-1.jpg` and cmd.exe treats a bare `&` as a command separator,
which silently truncated the path and killed the first run at file one.

## Delivery to clients

Skyelar already pays $10/mo for **Pixieset**, which solves client galleries
and downloads. Not rebuilding that. The site is a portfolio, not a delivery
system.

## Rejected

- **ImageKit** in front of R2 — the original plan. Dropped for native
  Cloudflare transformations: one vendor, one bill, one cache, and the free
  tier covers this site's scale.
- `not_found_handling: "single-page-application"` — every route is
  prerendered, so the 404 page is correct.
- `generateStaticParams()` returning `[]` — fails the build under
  `output: export`. The journal switch moves the route folder instead.

## Open items

- [ ] Reduce Austin's access on Skyelar's account to Administrator
- [ ] Covers for 002-004 are just the first frame of each set; Skyelar should choose
- [ ] Real prices; all four services currently say "Inquire"
- [ ] Real locations and years for series 002–004 (counts are now real)
- [ ] Real portrait of Skyelar for /about (currently a car frame from 001)
- [ ] `www` → apex redirect (currently both serve 200 = duplicate content)
- [ ] Journal: `npm run journal:on` when there is an entry to publish
- [ ] Contact form is UI-only; needs a backend (would require adding `main`)
